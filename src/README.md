# Legacy frontend source

The supported frontend is now `/frontend`.

Docker Compose builds `/frontend` through `Dockerfile.frontend`; do not add new
features under this root-level `src` directory. It is retained temporarily as
reference material while the team removes the duplicate prototype in a future
cleanup commit.
